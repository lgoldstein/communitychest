CREATE TABLE restful_data (
	id BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1),
	version INTEGER NOT NULL,
	balance INTEGER NOT NULL,
	lastModified TIMESTAMP(2) WITHOUT TIME ZONE NOT NULL,

	CONSTRAINT restful_data_pk PRIMARY KEY(id)
);